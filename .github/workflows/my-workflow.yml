name: My First Workflow

on: push

jobs:
    job-1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: "Hello from step1"
            - name: step2 
              run: "Hello from step2"

    job-2:
        runs-on: ubuntu-latest
        steps:
            - name: final step
              run: "Hello from final step job2"
                   