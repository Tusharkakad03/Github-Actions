name: My First Workflow

on: push

jobs:
    job-1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo "Hello from step1"
            - name: step2 
              run: echo "Hello from step2"

    job-2:
        runs-on: ubuntu-latest
        steps:
            - name: final step
              run: echo "Hello from final step job2"
                   